#!/bin/bash
#
# Makes a .zip for submission

id=24

# removes .o
make clean

# removes log files
rm *.out

# removes executable
rm da_proc

# zip to submit
zip -r ${id}.zip . -x *.git* *.vscode* *.DS_Store*